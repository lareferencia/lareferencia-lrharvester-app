# SQLite Catalog Configuration

# Tamaño del batch para inserts (default: 5000)
catalog.batch.size=5000

# Timeout de conexión en segundos
catalog.connection.timeout=30

# Habilitar WAL mode para mejor concurrencia de lectura
catalog.sqlite.wal-mode=true
